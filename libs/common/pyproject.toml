[project]
name = "common"
version = "0.1.0"
description = "Shared technical utilities for FTGO Python services"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "pydantic-settings>=2.6.0",
  "structlog>=24.4.0",
  "aio-pika>=9.4.0",
  "sqlalchemy>=2.0.36",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/common"]
